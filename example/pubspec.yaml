name: example
description: Annotates data classes for XML deserialization
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  docx_extractor:
    path: ../extractor/

dev_dependencies:
  build_runner: ^1.0.0
  build_test: ^0.10.3
  test: ^1.14.4
  parse_gen:
    path: ../parse_gen/
