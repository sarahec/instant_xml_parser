name: parse_gen
description: Annotates data classes for XML deserialization
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  analyzer: "0.39.14" # Pin the analyzer version to work around a generation failure
  build: ^1.3.0
  build_config: ^0.4.0
  recase: ^3.0.0
  source_gen: ^0.9.6
  xml: ^4.4.1
  parse_tools:
    path: ../parse_tools/

dev_dependencies:
  build_runner: ^1.0.0
  build_test: ^0.10.3
  pedantic: ^1.9.0
  test: ^1.14.4
