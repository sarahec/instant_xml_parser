name: runtime
description: Extracts text from .docx format files (e.g. Microsoft Word)
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.0 <3.0.0'

dependencies:
  async: ^2.4.0
  build: ^1.3.0
  build_config: ^0.4.0
  built_collection: ^4.3.1
  built_value: ^7.1.0
  xml: ^4.4.1

dev_dependencies:
  build_runner: ^1.0.0
  build_test: ^1.2.1
  built_value_generator: ^7.1.0
  pedantic: ^1.9.0
  test: ^1.14.4
