name: docx_extractor
description: Extracts text from .docx format files (e.g. Microsoft Word)
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  xml: ^4.4.1

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
